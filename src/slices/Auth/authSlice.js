import { createSlice } from "@reduxjs/toolkit"

// Set the initial stae for the slice
const initialState = {

}

export const authSlice = createSlice({
    name: 'auth',
    initialState: initialState,
    reducer: {
        
    }
})


export default authSlice.reducer;