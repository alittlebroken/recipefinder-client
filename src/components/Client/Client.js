import { Routes, Route } from 'react-router-dom'
import Layout from '../Layout/Layout'

const Client = () => {
  return (
    <Layout>
      <Routes>
        
      </Routes>
    </Layout>
  )
}

export default Client;