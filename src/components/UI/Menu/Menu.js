import './Menu.css'

const Menu = (props) => {

    /* Destructure the passed in props */
    const { 
        title
    } = props

    return (
        <>
        
        </>
    )

}

export default Menu