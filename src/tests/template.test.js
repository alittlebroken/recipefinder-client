import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom'

// Import components we are testing

xdescribe('<Component />', () => {

  xit('renders the component', async () => {
    
    // Setup

    // Execute
    //render(<Component />)

    // Assert

  })

  xit('renders the component and contains some text', async () => {
    
    // Setup

    // Execute
    //render(<Component />)

    // Assert
    //expect(screen.getByRole('heading')).toHaveTextContent('Hello World! Test Page')

  })

})