import axios from 'axios';

import authProvider from '../providers/authProvider'

jest.mock('axios')

describe('authProvider', () => {

    afterEach(() => {
        jest.resetAllMocks()
    })

    

})